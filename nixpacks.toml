[phases.setup]
aptPkgs = [
    "ffmpeg",
    "libsm6", 
    "libxext6",
    "libxrender-dev",
    "libglib2.0-0",
    "chromium", 
    "chromium-chromedriver",
    "xvfb"
]

[phases.build]
nixPkgs = [
    "chromium",
    "ffmpeg"
]

[start]
command = "python -m uvicorn web_ui:app --host 0.0.0.0 --port $PORT"
